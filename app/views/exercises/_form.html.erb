<div>
  <%= form_for(@exercise) do |f| %>
    <fieldset>
      <legend><%= @task.name %>　打卡</legend>
      <%= f.hidden_field :task_id, :value => @task.id %>
      <div class="task-description">
        <%= @task.description %>
      </div>

      <label>日期</label>
      <%= f.text_field :date, :class => "js-date-field" %>
      <label>内容</label>
      <%= f.cktext_area :content, :class => "ckeditor" %>
      <br>
      <%= submit_tag "提交" %>
    </fieldset>
  <% end -%>
</div>
