<div>
  <%= form_for(@reply) do |f| %>
    <fieldset>
      <%= f.hidden_field :topic_id %>
      <%= f.text_area :content, :class => "form-control reply", :rows => "4" %>
      <br>
      <%= submit_tag "提交", :class => "btn btn-primary" %> <%= link_to '返回', topics_path, :class => "btn btn-default" %>
    </fieldset>
  <% end -%>
</div>
